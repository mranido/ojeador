'use strict';

const createContact = require('./crete-contact');
const acceptContact = require('./accept-contact');
const rejectContact = require('./reject-contact');

module.exports= {createContact, acceptContact, rejectContact};